Scripts that initialise an Odoo database with data using ERPpeek.

Because ERPpeek pushes data to Odoo via it's XML-RPC API, the Odoo server must first be up and running to receive the database updates.

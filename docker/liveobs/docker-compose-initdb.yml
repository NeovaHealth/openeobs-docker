version: '2'
services:
  web:
    environment:
      - GO_PIPELINE_COUNTER=${VERSION}
    build:
      context: testing
      dockerfile: Dockerfile-testing
      args:
        version: ${VERSION}
    entrypoint: /opt/odoo/createdb.sh
